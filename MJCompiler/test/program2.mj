program p
const int size = 10;

record Board {
int rows, columns;
}
Table val;
Board board;
{
void f(char ch, int a, int arg)
int x;
{
x = arg;
}
void mojaFunkcija1(int a, int...go){}
void mojaFunkcija1(int...go){}
void main() int x, i; char c;
{
val = new Table;
val.pos = new int [size];
val.neg = new int [size];
read(x);
if(x > 5)
{
goto skip;
}
i = 0;
do{
val.putp(0, i); 
 val.putn(0, i);
 i++;
}while(i<size);
skip: f(c, x, i);
board = new Board;
read(board.rows);
x = board.rows;
do{
 if(x <= 0) break;
 if (0 <= x && x < size)
{
val.putp(val.getp(x)+1);
} 
else if (‐size < x && x < 0)
 {
val.putn(val.getn(‐x)+1);
 }
read(x);
 }while(true);
}
}